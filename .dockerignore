.git/
.venv/

# Checkpoints — generated at runtime, not needed in the image
*.pt

# Exclude all local data files, then re-include runtime-required datasets
data/
!data/tiny_shakespeare.txt
!data/sft_data.json
!data/grpo_prompts.json

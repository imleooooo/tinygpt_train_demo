.git/
.venv/

# Checkpoints — generated at runtime, not needed in the image
*.pt

# Exclude all local data files, then re-include only the checked-in datasets
data/
!data/sft_data.json
!data/grpo_prompts.json
